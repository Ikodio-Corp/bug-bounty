# 🎯 IKODIO BugBounty - Production Server Status

##  DEPLOYMENT BERHASIL!

Aplikasi IKODIO BugBounty telah berhasil di-deploy ke production server.

---

## 📡 Server Information

| Parameter | Value |
|-----------|-------|
| **IP Address** | 192.168.100.6 |
| **SSH Port** | 7420 |
| **Username** | ikodioxlapo |
| **OS** | Ubuntu 24.04.3 LTS |
| **Location** | /home/ikodioxlapo/ikodio-bugbounty |

---

## 🌐 Service URLs

### IKODIO BugBounty (NEW)
- 🌟 **Frontend:** http://192.168.100.6:3003
- 🔌 **Backend API:** http://192.168.100.6:8001
- 📚 **API Docs:** http://192.168.100.6:8001/api/docs
- �� **Health Check:** http://192.168.100.6:8001/health

### Services Lain di Server (Already Running)
- 💬 **Rocket.Chat:** http://192.168.100.6:3000
- � **IKODIO ERP:** http://192.168.100.6:8000 (Django/Gunicorn)
- 📋 **Other Apps:** Port 3001, 3002, 8002

---

## 🚀 Quick Access

### Dari Komputer di Network 192.168.100.x

**Direct Access:**
1. Buka: http://192.168.100.6:3003
2. Check backend: http://192.168.100.6:8001/health

### Dari Komputer Luar (Mac/Windows)

**Via SSH Tunnel:**
```bash
# Terminal 1 - Create tunnel
sshpass -p 'Mi252512@' ssh -p 7420 -L 3003:localhost:3003 -L 8001:localhost:8001 ikodioxlapo@192.168.100.6

# Biarkan terminal ini running, lalu:
# Buka browser: http://localhost:3003
# Backend: http://localhost:8001
```

**Via SSH Login:**
```bash
ssh -p 7420 ikodioxlapo@192.168.100.6
# Password: Mi252512@
```

---

## 📊 Service Management

### Check Status
```bash
ssh -p 7420 ikodioxlapo@192.168.100.6 << 'EOF'
# Check backend
ps aux | grep start_simple | grep -v grep
curl -s http://localhost:8001/health

# Check frontend  
ps aux | grep "npm.*3003" | grep -v grep
curl -I http://localhost:3003
EOF
```

### Start Services
```bash
ssh -p 7420 ikodioxlapo@192.168.100.6 << 'EOF'
# Backend
cd ~/ikodio-bugbounty/backend
source venv/bin/activate
nohup python3 start_simple.py > backend.log 2>&1 &
echo $! > backend.pid

# Frontend
cd ~/ikodio-bugbounty/frontend
PORT=3003 nohup npm start > frontend.log 2>&1 &
echo $! > frontend.pid
EOF
```

### Stop Services
```bash
ssh -p 7420 ikodioxlapo@192.168.100.6 << 'EOF'
# Kill backend
fuser -k 8001/tcp

# Kill frontend
fuser -k 3003/tcp
EOF
```

### View Logs
```bash
# Backend logs
ssh -p 7420 ikodioxlapo@192.168.100.6 'tail -f ~/ikodio-bugbounty/backend/backend.log'

# Frontend logs
ssh -p 7420 ikodioxlapo@192.168.100.6 'tail -f ~/ikodio-bugbounty/frontend/frontend.log'
```

---

## 🔄 Update/Redeploy

Dari local machine:
```bash
cd /Users/hylmii/Documents/ikodio-bugbounty

# 1. Commit changes
git add .
git commit -m "Update: your changes"

# 2. Deploy to server
./deploy-to-server.sh

# 3. Restart services
ssh -p 7420 ikodioxlapo@192.168.100.6 << 'EOF'
# Kill old processes
fuser -k 8001/tcp 3003/tcp

# Start new
cd ~/ikodio-bugbounty/backend
source venv/bin/activate
nohup python3 start_simple.py > backend.log 2>&1 &

cd ~/ikodio-bugbounty/frontend
PORT=3003 nohup npm start > frontend.log 2>&1 &
EOF
```

---

## 🔧 Configuration

### Backend (.env)
```bash
# Location: ~/ikodio-bugbounty/backend/.env
DATABASE_URL=postgresql://ikodio:Mi252512@localhost/ikodio_bugbounty
REDIS_URL=redis://localhost:6379/0
SECRET_KEY=<auto-generated>
PORT=8001
```

### Frontend (.env.local)
```bash
# Location: ~/ikodio-bugbounty/frontend/.env.local
NEXT_PUBLIC_API_URL=http://192.168.100.6:8001/api
NEXT_PUBLIC_WS_URL=ws://192.168.100.6:8001/ws
```

---

## 🐛 Troubleshooting

### Frontend tidak tampil
```bash
ssh -p 7420 ikodioxlapo@192.168.100.6
cd ~/ikodio-bugbounty/frontend
tail -50 frontend.log
```

### Backend tidak response
```bash
ssh -p 7420 ikodioxlapo@192.168.100.6
cd ~/ikodio-bugbounty/backend
tail -50 backend.log
curl http://localhost:8001/health
```

### Port sudah digunakan
```bash
# Check port usage
lsof -i:8001  # backend
lsof -i:3003  # frontend

# Kill process
fuser -k 8001/tcp
fuser -k 3003/tcp
```

### Database connection error
```bash
# Check PostgreSQL
sudo systemctl status postgresql

# Check Redis
redis-cli ping
```

---

## 📝 Important Notes

1. **Port Allocation:**
   - Port 3000: Rocket.Chat (existing)
   - Port 8000: IKODIO ERP (existing)
   - Port 3001-3002: Other Next.js apps
   - **Port 3003: IKODIO BugBounty Frontend** 
   - **Port 8001: IKODIO BugBounty Backend** 

2. **Server sudah running:**
   - Rocket.Chat workspace
   - IKODIO ERP system
   - Wekan board
   - MongoDB, PostgreSQL, Redis

3. **Credentials:**
   - SSH: ikodioxlapo / Mi252512@
   - DB: ikodio / Mi252512@
   - Jangan share credentials ini!

4. **Next Steps:**
   - [ ] Setup nginx reverse proxy untuk domain
   - [ ] Enable SSL/TLS dengan Let's Encrypt
   - [ ] Setup systemd services untuk auto-restart
   - [ ] Configure database backup automation
   - [ ] Setup monitoring (Prometheus/Grafana)

---

## 🎉 Success Criteria

 Backend running di port 8001
 Frontend running di port 3003  
 Database connected (PostgreSQL)
 Redis connected
 API endpoints responding
 Health check passed
 No port conflicts

---

**Deployed:** November 20, 2025
**Status:**  PRODUCTION READY
**Version:** 1.0.0
